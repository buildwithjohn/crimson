import DashboardShell from "@/components/dashboard/DashboardShell";

export const metadata = {
  title: "CrimsonWings OS — Mission Control Dashboard",
  description: "CrimsonWings Plasma Biologics Ltd — Internal operations platform. Blood inventory, delivery tracking, hospital requests, and analytics.",
};

export default function DashboardPage() {
  return <DashboardShell />;
}
